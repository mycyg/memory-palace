# 基于事件的 Agent 记忆系统 —— 设计文档

版本：v0.1（2026-08-25）
状态：讨论稿，待定名

**一句话**：事件是记忆的原子。agent 的每段工作记录为「意图 → 行动 → 结果」的闭环；原文不可变，索引可重建，整理离线进行，注入受预算约束。

---

## 1. 问题与目标

### 1.1 现状的问题

当前主流的上下文管理方式是压缩（compact）：上下文接近上限时，由模型对历史生成摘要，替换原文。该方式有三个结构性缺陷：

1. **优化目标错位。** 压缩发生在任务进行中，摘要为「继续当前任务」优化，不为「未来会话的检索」优化。两个目标在取舍上冲突：续命摘要保留当前状态，丢弃过程与理由。
2. **丢弃不可重建信息。** 决策理由、被放弃的方案、用户口头给出的约束，只存在于对话原文中。压缩后无法恢复，而这三类信息恰是后续会话最常需要的。
3. **无跨会话结构。** 压缩产物随会话结束消失。每个新会话从零开始，agent 重复踩坑、重复询问已确认过的约束。

### 1.2 设计目标（按优先序）

1. **犯过的错不再犯第二次**：失败路径与坑被记录、可召回。
2. **决策可追溯**：为什么这样做、为什么没那样做，随时可查。
3. **跨会话接续**：新会话启动时直接获得「上次做到哪、接下来做什么」。
4. **上下文占用有硬上限**：记忆注入的 token 总量受固定预算约束，不随记忆总量增长。

### 1.3 非目标

- 不追求全量记忆。原则是**宁漏勿胀**：漏是自愈的（漏 → 犯错 → 错被记录 → 下次在索引中），胀不自愈（无法判定该删什么）。该原则的适用前提是场景有兜底机制（测试、review、人工确认），错误代价可控。
- 不存储可从 repo 重建的知识（调用图、接口清单、目录结构）。这类信息由静态分析工具实时提供，记忆只存不可重建的部分。
- 不替代对话原文。压缩只发生在索引层，不发生在存储层。

---

## 2. 核心模型：事件

### 2.1 定义

一个事件是一个闭环：

```
意图（intent） → 行动序列（actions） → 结果（outcome）
```

事件在结果处闭合。结果分三种：完成（done）、放弃（abandoned）、被推翻（superseded）。事件允许嵌套：子事件挂在父事件下，各自闭环。

选择闭环作为原子单位的理由：

- **检索形状匹配**：召回请求的自然形式是「上次 X 是怎么处理的、结果如何」，即按意图检索、按结果取值。
- **自带价值标签**：完成的事件沉淀为可复用路径，放弃的事件沉淀为坑。经验（lesson）是事件的派生物，不需要独立的经验模块。
- **可整取整弃**：闭环自包含，注入与清退都以事件为单位，这是上下文预算（第 5 节）可执行的前提。

### 2.2 事件的两种来源

| 来源 | 机制 | 典型场景 |
|---|---|---|
| **声明式**（planned） | 计划先行：plan / todo 中写下一项任务即开启事件，验收即闭合 | 新工程、模块构建 |
| **检测式**（reactive） | 信号先行：报错出现、测试失败、用户打断，即开启事件；信号消失即闭合 | 修 bug、故障处理 |

两者互相转化：检测到的问题可催生新的声明（发现 bug → 加入 todo）；声明的任务执行中会产生检测式子事件。

**实现要点**：agent 的 todo 系统本身就是声明式事件流。todo 的生命周期（pending → in_progress → completed）与事件生命周期同构，记忆系统只需持久化这个已有结构，不需要发明事件切分器。

### 2.3 事件类型（kind）

coding 场景的默认枚举：

| kind | 闭环形态 | 备注 |
|---|---|---|
| `decision` | 问题 → 备选 → 选定＋理由 | 选型、架构、约定。代码只显示结论，不显示理由与被否方案，此类事件的信息不可重建 |
| `build` | 计划 → 实现 → 验收 | 模块从无到有，对应 todo 条目 |
| `explore` | 假设 → 尝试 → 放弃＋原因 | 废弃方案在 repo 中几乎无痕迹，此类事件最易漏、复用价值最高 |
| `fix` | 信号 → 定位 → 消除 | 检测式为主 |

kind 为开放枚举，跨场景可替换（见第 6 节）。

### 2.4 状态机与不可变原则

```
open ──→ done
     ──→ abandoned
（任意状态）──→ superseded(by: <event-id>)
```

`superseded` 可从任意状态迁入：已完成的决策同样可以被事后推翻（done → superseded 是常态而非例外——「用 SQLite」完成三个月后改用 Postgres）。推翻不抹除原 outcome：事件当时的结果仍是历史事实。

**事件一经写入不修改、不删除。** 推翻一个决策时，旧事件标记 `superseded` 并链接到新事件。理由：

- 「当时为什么那样想」是重构时最常回查的信息，删除即丢失。
- 不可变存储使一切整理操作（第 4.3 节）幂等、可重跑、可从原文重建。
- 事件描述的是已发生的过程，时态为完成时，不随代码状态过时（对比：描述状态的文档会过时）。

### 2.5 事件 schema

```yaml
id: 2026-08-25_143201          # 时间戳即 id，天然有序
parent: 2026-08-25_140000      # 事件树；顶层事件为 null
kind: fix                      # decision | build | explore | fix（开放枚举）
status: done                   # open | done | abandoned | superseded
superseded_by: null            # status 为 superseded 时指向新事件
intent: >
  多个训练任务的 Ray 抢占同一端口，导致任务启动失败
anchors:                       # 客观锚点，检索与审计用
  commits: [a3f21c9]
  files: [train/launcher.py]
  tests: []
  dialog: [session-0817#L220-L410]   # 对话原文区间指针
outcome: >
  为每个任务分配独立端口区间，冲突消除
lesson: >                      # 蒸馏；可空。多数事件不需要
  并行启动多个 Ray 任务时，端口按任务 id 错开分配；使用默认端口必然冲突
```

正文（frontmatter 之下）可选：行动序列摘要，3–5 行。原始过程不复制进事件文件，通过 `anchors.dialog` 指向对话原文。

字段纪律：**多数事件不配有 lesson。** lesson 是常驻候选，写入门槛高于 outcome（见 8.3 错误蒸馏的毒性）。

---

## 3. 三层架构

```
┌─────────────────────────────────────────────┐
│ L2 注入层（进上下文的部分，固定预算）           │
│   固定规则 ＋ 工作集 ＋ 按需检索结果            │
├─────────────────────────────────────────────┤
│ L1 索引层（派生，可整体重建）                  │
│   工作集 / 项目索引 / lesson 表               │
├─────────────────────────────────────────────┤
│ L0 存储层（append-only，不可变）              │
│   事件文件 ＋ 对话原文 ＋ 锚点                 │
└─────────────────────────────────────────────┘
```

### L0 存储层

- 一个事件一个文件（markdown ＋ frontmatter），追加写，不改写。
- 对话原文由宿主（如 Claude Code 的 transcript）保存，事件通过区间指针引用，不复制。
- 层级目录不进入存储层：存储扁平，避免预设目录树随项目结构变化腐烂。

### L1 索引层

- 全部内容由 L0 派生，可随时删除重建。
- **工作集（working-set）**：第一层索引，固定 token 预算（建议 1–2k）。内容 ＝ open 事件 ＋ 预取区（预测 pass 的产出，见 §4.3，占比默认 1/3）＋ 晋升 lesson ＋ 最近闭合事件的 outcome 行。三种时态的混合：open 是现在进行时，预取是将来时，lesson 与 outcome 是完成时的精华。
- **项目索引**：全量事件的单行索引（id ＋ kind ＋ status ＋ intent 一行摘要），分层汇总，在盘可查，不常驻上下文。
- **lesson 表**：已蒸馏经验的汇总，每条链回来源事件。重复出现的 lesson 是晋升候选。

层级是索引的视图，不是存储的结构。同一批事件可以按模块、按时间、按 kind 生成不同视图，视图过时即重建。

**粒度也是视图**：事件写入时的粒度（todo 写得过粗或过细）不在存储层修正——L0 不可变。深整理在索引层做粒度调节：碎事件聚合为组（组行代替成员行进入工作集与浮现），粗事件生成虚拟分段（浮现命中某文件锚点时展示对应分段而非整体结论）。合并与拆分都是派生物，检测规则变了就重算，L0 一字不动。

### L2 注入层

进入上下文的只有三样：

1. **固定规则**：从 lesson 晋升的常驻约束（等价于 CLAUDE.md 的角色），有淘汰机制（第 4.3 节）。CLAUDE.md 本身就是人工维护的固定注入层——本系统的晋升机制为它供货：反复验证的 lesson 生成可粘贴的建议文件，采纳与否由人裁决，系统永不自动修改用户的 CLAUDE.md。无感的边界在这里：读不需要授权，写入人的文档需要。跨项目的用户级 lesson 是同一机制的更高一层，晋升前须通过可移植性判定（不含项目特有信息）并经过敏感信息清洗。
2. **工作集**：会话启动时注入，预算内。
3. **线索浮现与兜底检索结果**：主路径为锚点触发的主动浮现，query 检索仅兜底；均默认 outcome 单行，显式请求才下钻原文（第 4.4 节）。

---

## 4. 生命周期

### 4.1 写入（flush）

写入是同步操作，只写不整理：

- **会话中**：todo 状态变化、commit、测试结果等锚点信号实时追加。
- **compact 前**：compact 是被迫遗忘的时刻，触发抢救式 flush——将未落盘的 open 事件状态与锚点写入 L0。此时不做任何整理。
- **会话结束**：最后一次 flush，随后触发轻整理。

类比：WAL 随时写，合并是后台进程，两者不在同一时刻发生。

### 4.2 闭合

事件闭合的判定信号，按可靠性排序：

1. todo 条目标记 completed（声明式验收）
2. 客观锚点：测试由失败转通过、报错消失、commit 完成
3. 用户明确确认
4. 超时降级：open 状态超过 N 天（默认 14）的事件，深整理时标记 stale，提示确认或归入 abandoned

### 4.3 整理（离线，即 dreaming）

整理分两级，均在离线进行：

| | 轻整理 | 深整理 |
|---|---|---|
| 触发 | 每次会话结束 | 脏量达标（未整理事件 ≥ N 或事件树发生重组），且无活跃会话 |
| 耗时 | 秒级 | 分钟级 |
| 模型 | 小模型或纯规则 | 大模型 |
| 内容 | 闭合悬挂事件；为新闭合事件补 outcome 与显著性先验；规则级预取；重建工作集 | 跨事件去重；蒸馏与重蒸馏 lesson；晋升（重复 lesson → 固定规则；可移植 lesson → 用户级候选）；失效检查（superseded 链、过时规则退休）；浮现采纳判定与显著性后验重算；粒度调节（合并组／虚拟分段）；模型级预取；CLAUDE.md 建议生成；重排项目索引 |

**预测 pass（预取）**：两级整理的收尾、重建工作集之前，执行下次会话入口的预测，将预测关联的已闭合事件提升进工作集的预取区。信号源按强度排序：①对话中的将来时表述（「下次先做 X」），抽取时捕获为前瞻标记（prospective marker），挂为特殊 open 事件；②open 事件的关联物（同锚点历史事件、parent 树兄弟、相关 lesson，经锚点倒排反查）；③最近事件的锚点聚类。轻整理做规则级预取（①②），深整理做模型级预测（③及综合）。预取区占工作集预算的固定份额（默认 1/3），按置信度排序截断——预测错误的代价被预算封顶。预取只提升索引层级、不改动 L0 与事件字段，为可逆的视图操作。预取命中率可测（预取锚点与下次会话实际锚点的交集），命中强化、落空衰减，预测路径自校准。

**触发条件的设计**：脏量是触发条件，空闲只是许可条件。固定时间间隔（如「空闲 6 小时后整理」）不成立：空闲时长是「会话已结束」与「有货可整理」两个条件的不完美代理，直接检测这两个条件即可。会话结束信号取：会话关闭／归档、新会话开启（后者同时是预取的最佳时机）。

**安全纪律（无人值守的前提）**：整理只写 L1 派生层与事件的 lesson 字段，**永远不修改事件原文的意图、行动、结果记录**。由此获得：

- 幂等：整理可无限重跑，输入始终是不可变的 L0。
- 可中止：整理中途被新会话打断即中止，旧索引仍可用。
- 原子替换：新索引完整写出后 rename，不存在中间态。
- 可恢复：整理产物质量不佳时，调整 prompt 重跑，最坏情况等于从未整理。

### 4.4 召回：联想优先，检索兜底

本设计**不采用 query 式 RAG**（query → embedding 相似度 → top-k 注入）作为主召回路径。理由：

1. **query 模式假设 agent 知道自己缺什么。** 坑的特征恰是 agent 不知道它存在，因此不会发起对应检索。最需要记忆的时刻，正是 agent 意识不到需要检索的时刻。
2. **相似 ≠ 相关。** 事件记忆需要的关联是「同一个对象」（同文件、同报错、同模块），可精确匹配；embedding 相似度引入不可控的模糊性，且无法解释。
3. **chunk 化破坏闭环。** 事件切片后检索返回碎片，意图与结果分离，自包含性（整取整弃的前提）丢失。

**主路径：线索触发的主动浮现（push）。** 记忆系统监听 agent 的行为流，命中锚点即注入对应事件的 outcome 单行，不依赖 agent 发起请求：

| 触发线索 | 匹配方式 | 浮现内容 |
|---|---|---|
| agent 读／改某文件 | `anchors.files` 精确匹配 | 该文件关联事件的 outcome 行 |
| 新 todo 开启 | intent 文本匹配历史事件 intent 与 lesson 触发条件 | 相关 lesson、同类事件 outcome |
| 报错出现 | 错误签名匹配历史 `fix` 事件 | 该错误的历史处置路径 |
| 会话启动 | — | 工作集整体注入 |

对应人的记忆机制：人极少检索记忆，人被环境线索触发联想（cue-based recall）——走进房间想起这里发生过什么。锚点触发是这一机制的工程化，覆盖「agent 不知道自己不知道」的场景。

**浮现的预算纪律**：单次触发注入不超过 K 行（默认 3），格式为 `[event-id] outcome 单行`；候选超额时按新近度与锚点强度排序截断；同一事件在同一会话内只浮现一次。浮现不足由兜底检索补齐，宁漏勿胀。

**兜底：显式检索。** 服务于 agent 明确的考古需求，分层召回纪律：

1. `memory_search(query)` 返回匹配事件的 outcome 单行（含 id），匹配用 grep / BM25 级文本匹配，不用向量（见 7.4）。
2. `memory_read(id)` 才返回事件全文。
3. 事件全文仍不含过程原文；审计经 `anchors.dialog` 读对话原文。

lesson / outcome 的存在目的就是让约九成召回停止在单行层。原文是审计层，不是工作层。

**上下文隔离模式**：兜底检索可整体委托给子 agent——子 agent 读多个事件全文，向主线程只返回结论摘要，检索过程的上下文成本隔离在子进程内。

### 4.5 遗忘

原则：**遗忘压缩的是访问结构，不是信息。** 事件文本的存储成本可忽略（重度使用一年约数 MB）；随记忆总量增长而爆炸的是索引规模与整理扫描成本——遗忘机制约束的是后者。

四级生命周期，每降一级，系统为该记忆付出的运行成本递减到零（判据吃显著性系统的现成数据——遗忘是显著性的自然延伸）：

| 级 | 判据 | 状态 |
|---|---|---|
| hot | 默认 | 进索引、可浮现、参与检索 |
| cold | 长期零引用、零浮现命中、低显著性 | 逐出全部索引，归档索引留单行；兜底考古可达，热路径成本归零 |
| frozen | cold 满一年 | 按季度生成**纪元摘要**（一段时代总结＋成员清单），原文件整包归档移出事件目录；磁盘约压至 1/5，整理扫描范围永久有界 |
| purged | 永不自动 | 仅手动命令，默认 dry-run |

- open 事件、前瞻标记、晋升 lesson 的来源事件永不冷却；被活跃 superseded 链引用的事件永不冻结（链目标必须可读）。
- 冻结是文件系统层的搬移，不是内容层的删改：包内原文完整，`thaw` 随时解冻重入索引。不可变原则的代价重新定价——对 frozen 层的审计与重跑需先解冻，属刻意取舍。
- 固定规则的退休照旧：连续 M 次深整理未被引用的规则移回 lesson 表。
- 认知对应：systems consolidation——远期记忆随时间语义化、图式化（gist）。近事记细节，远事记时代；纪元摘要就是 gist 层，episodic → semantic 巩固梯度的最后一段。

实现见 SPEC §3.19。

### 4.6 显著性（salience）

优先度不决定「记不记」，决定「住哪层」——L0 全量保证不漏，索引层按显著性选择保证不胀，两个目标在不同层各自满足。结构为三层：**规则划界，自评开局，证据说了算。**

1. **规则做硬边界**（场景插件、出厂默认）：decision 保底、顺利 build 封顶，垂直场景的合规规则（如药物警戒的 seriousness × expectedness 分级）直接置顶。
2. **自评做先验**：事件闭合时模型给出一档评级与一句理由，写入事件文件——「当时认为它重要」是完成时事实，不违反不可变，且随模型能力提升而更准。
3. **证据做后验**：引用数、浮现命中／被无视、是否触发 supersede、是否 stale，深整理时以证据修正先验得到最终显著性。

后验修正不可省——重要性的完整信息在事件发生时不在场（后果尚未发生），这是时序问题而非模型能力问题；且在线自评有峰终偏差，自评分直接决定去留还会诱发自我强化（写记忆与用记忆是同一 agent），而「被后续行为引用」产生于别的时刻，难以被优化。最终显著性存放于索引层（随时间变化的量不进不可变的 L0），供工作集填充、浮现排序、蒸馏候选选择共用（实现见 SPEC §3.11）。

---

## 5. 上下文预算

挂载过量拆解为三个独立问题，分别处理：

| 问题 | 机制 | 解法 |
|---|---|---|
| **入口过大** | 索引随事件总量膨胀 | 工作集固定预算（1–2k token），常驻的是工作集而非全量索引；全量索引在盘可查 |
| **拉取过度** | 检索工具被高频调用，每次全文进上下文 | 主路径改为线索浮现（push，单行、限 K 条），消除多数主动拉取；兜底检索分层召回：默认单行，显式下钻；子 agent 隔离 |
| **驻留过久** | 读入的内容用完不释放 | 宿主的工具结果清退（context editing / microcompact）；以事件为单位整取整弃 |

预算是硬约束：工作集生成时按优先级填充至预算即止，超出部分留在项目索引中等待检索。注入量不随记忆总量增长。

---

## 6. 通用化：骨架与插件

事件记忆不依赖 coding 场景，依赖的是任务结构（agency）：只要交互具有「意图 → 行动 → 结果」的形态，闭环即成立。无任务结构的交互（纯问答、闲聊）退化为事实抽取，不在本系统范围内。

| | 骨架（跨场景不变） | 插件（按场景实现） |
|---|---|---|
| 组成 | 闭环 schema、三层架构、两级整理、上下文预算、不可变原则 | 锚点检测器、kind 枚举、蒸馏方向、检索权重 |

场景插件示例：

| 场景 | 锚点 | kind 示例 | 蒸馏方向 | 检索加权 |
|---|---|---|---|---|
| coding | commit、测试、报错 | decision / build / explore / fix | 坑、约束、决策理由 | 同文件 |
| 客服 | ticket 状态 | inquiry / complaint / refund | 客户偏好、历史承诺 | 同客户 |
| 研究 | 问题被回答／搁置 | question / read / synthesize | 已排除的假设、来源可信度 | 同主题 |
| 运维 | alert 触发／恢复 | incident / change / drill | 故障根因、处置路径 | 同服务 |
| 角色扮演 | 无客观锚点，LLM 判定情节闭合 | scene / promise / conflict | 关系状态、人设事实 | 同角色、情感强度 |

锚点客观性决定事件切分的自动化程度：coding 与运维可程序化检测；角色扮演依赖 LLM 判断，是最难的场景而非最易的。

---

## 7. MVP 实现路径（宿主：Claude Code）

### 7.1 选择 Claude Code 作为第一宿主的理由

- hooks 覆盖全部所需时机：`SessionStart`（注入）、`PreCompact`（抢救 flush）、`SessionEnd`（flush ＋ 轻整理）、`PostToolUse`（捕获 TodoWrite 与 git 操作）。
- TodoWrite 已提供声明式事件流，transcript（jsonl）已提供对话原文与区间定位。
- 文件系统足以支撑 L0/L1，零外部依赖。

### 7.2 目录布局

```
<project>/.memory/
├── events/                      # L0：一事件一文件
│   └── 2026-08-25_143201.md
├── index/                       # L1：全部可重建
│   ├── working-set.md           # 工作集（注入用）
│   ├── project.md               # 全量单行索引
│   └── lessons.md               # lesson 表（含晋升状态）
└── config.yml                   # 预算、阈值、权重
```

### 7.3 V0.1 数据流

| 时机 | Hook | 动作 |
|---|---|---|
| 会话启动 | `SessionStart` | 读 `index/working-set.md`，stdout 注入上下文 |
| todo 变化 | `PostToolUse` (TodoWrite) | 声明式事件开启／闭合，追加写 `events/`；新 todo 的 intent 匹配历史事件，命中则浮现 |
| 读／改文件 | `PostToolUse` (Read/Edit) | 查锚点索引，命中则以 additionalContext 注入该文件关联事件的 outcome 行（限 K 条） |
| commit / 测试 / 报错 | `PostToolUse` (Bash) | 锚点追加到对应 open 事件；报错签名命中历史 `fix` 事件则浮现处置路径 |
| compact 前 | `PreCompact` | 后台调用 `claude -p`（小模型）从 transcript 抽取未落盘事件，追加写 |
| 会话结束 | `SessionEnd` | 最后一次 flush；后台跑轻整理（闭合、补 outcome、重建工作集）；检查脏量，达标则跑深整理 |

浮现依赖一个锚点倒排索引（`index/` 内，文件路径／错误签名 → 事件 id 列表，纯文本、深整理时重建），hook 内查询为毫秒级，不调用模型。

兜底检索通过一个 skill 或 MCP 工具暴露：

- `memory_search(query)` → 匹配事件的 outcome 单行列表（读 `index/project.md` ＋ 文本匹配打分）
- `memory_read(id)` → 事件全文
- `memory_trace(id)` → 按 `anchors.dialog` 取对话原文区间

### 7.4 MVP 的两个反选择

- **不采用 query 式 RAG 主路径，也不用向量库。** 主召回是锚点精确匹配的线索浮现；兜底检索用 grep ＋ BM25，数千事件规模内足够，可解释、可手查、零依赖。向量检索若未来引入，仅作为兜底检索的补充排序信号，不改变「联想优先」的结构。
- **不做自动目录树。** 模块视图等分类视图由深整理按需生成进 L1，生成错了重建即可。

### 7.5 评估设计

四个指标，全部由运行埋点与 L0 直接计算，为论文 evaluation 与日常自检共用（`eventmem stats` 输出）：

| 指标 | 定义 | 数据来源 |
|---|---|---|
| 浮现命中率 | 浮现记录中，同会话后续 10 次工具调用内其文件锚点被编辑的比例（近似采纳判定，误差来源见 §8） | `log/surfaced-*.jsonl` ＋ feed／transcript，深整理离线判定 |
| 重复踩坑率 | 同一错误签名关联 ≥2 个 fix 事件的组数及其时间分布 | 直接扫 L0，无需埋点 |
| 上下文成本 | 每会话记忆注入字符量（工作集＋浮现），与全量挂载基线对比 | `log/injected-*.jsonl`、`log/surfaced-*.jsonl` |
| 接续成本 | 会话启动注入的非空率与预取命中率（预取锚点与实际锚点交集） | `log/prefetch-outcome.jsonl` |

埋点纪律：全部为本地 jsonl 追加写，不进上下文、不上传；判定在深整理离线执行，热路径零开销。

### 7.6 后续版本

- v0.2（已落地，SPEC §3.11–3.19）：显著性、预取、评估埋点、清洗、CLAUDE.md 建议、粒度自适应、子 agent 归属、跨项目晋升、负反馈、分级遗忘；第二宿主 dsh 原生插件（B-lite：TS 热路径查表＋Python 冷路径写入，`.memory/` 单写入方）。
- v0.3 候选：权重与阈值的实验校准（回放评估工具）；深整理的独立调度（launchd/cron 定时检查脏量，非定时整理）；dsh 插件 B-full（纯 TS 全移植，待宿主 API 出 rc 抖动期）；search 兜底的 outcome 进索引列（规模化）；第三场景插件验证（建议运维或研究）。

---

## 8. 开放问题

1. **浮现与排序参数**（v0.2 已实现显著性排序与负反馈通道，SPEC §3.11/3.13）：权重与衰减常量仍需实验校准；回放式评估工具待建。
2. **浮现的误报代价**（v0.2 已实现负反馈：浮现被无视 → evidence.ignored → 显著性降权）：残余问题是「采纳判定」的近似误差——以「后续编辑其文件锚点」近似采纳，会把巧合编辑计为 hit、把纯阅读式采纳计为 ignored；判定规则的精度本身需要人工抽检标定。
3. **事件粒度**（v0.2 已实现索引层的组合并与虚拟分段，SPEC §3.16）：检测阈值（连续 ≥3、锚点 ≥8）为经验值；LLM 组概括的质量未评估。
4. **错误蒸馏的毒性**：写错的 lesson 比没有 lesson 更糟——lesson 进入工作集常驻，错误会被反复应用。缓解：lesson 必须链回来源事件，agent 对 lesson 存疑时可查原文；深整理对 lesson 做复核；v0.2 增加显著性降权通道。是否足够，待验证。
5. **多项目与隐私**（v0.2 已实现用户级晋升，SPEC §3.18）：可移植性判定与敏感信息清洗（SPEC §3.14）构成信息流动边界，但 LLM 判定的保守度未标定——「存疑即不候选」依赖 prompt 遵循，需要抽检；企业场景（多用户共享项目）的边界另议。
6. **规模上限**：数千事件后项目索引本身需要分层汇总，汇总即有损压缩，如何保持「压缩只在索引层」的纪律同时控制索引深度，待设计。同层问题：search 的兜底检索目前每查询读全量事件文件，规模化需要 outcome 进索引列。
7. **子 agent 归属的深度**（v0.2 以委托事件浅归属，SPEC §3.17）：子 agent 内部的失败细节不进入主记忆，委托事件质量受返回摘要质量限制；解析子 agent 自己的 transcript 是否值得（成本 vs 信息增量），待观察。

---

## 9. 与既有工作的关系

| 系统 | 借鉴 | 本设计的区别 |
|---|---|---|
| Generative Agents（Park et al. 2023） | 反思机制、检索三因子（新近 × 重要 × 相关） | 记忆原子是带客观锚点的闭环事件，非观察流；重要性由结果与引用频次派生，非模型自评 |
| HippoRAG（2024） | 索引与内容分离（海马体索引理论） | 索引单位是事件而非实体三元组；检索目标是过程与理由，非事实 |
| MemGPT / Letta | 分层上下文管理 | 不做模型自主分页；注入受固定预算与工作集约束 |
| Zep / Graphiti | 时序失效（supersede 而非删除） | 以事件闭环为原子，非知识图谱三元组 |
| Voyager（2023） | 技能库（成功路径沉淀复用） | lesson 同时从失败蒸馏；晋升为常驻规则设门槛与退休机制 |
| ChatGPT memory | 事实抽取的形态参考 | 面向有任务结构的 agent 场景；保留闭环结构与原文指针 |
| 经典 RAG 流水线 | —— | 主召回为线索触发的主动浮现（cue-based），非 query → 相似度 → top-k；锚点精确匹配替代 embedding 相似度；query 检索仅兜底 |

---

## 附：定名候选

| 候选 | 出处 | 备注 |
|---|---|---|
| **REM** | Reflective Event Memory；REM 睡眠即做梦阶段 | 与「整理发生在睡眠中」的设计直接呼应 |
| **engram** | 记忆痕迹（神经科学术语，记忆的物理载体） | 与「事件是记忆的原子」呼应；有少量同名小项目 |
| **electric-sheep** | 《仿生人会梦见电子羊吗》 | 与本项目名互文；注意与 Scott Draves 的同名分布式屏保项目重名 |
